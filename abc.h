#include<stdio.h>
int main()
{
    int Ax,Ay;
    int Bx,Bx;
    int work;
   // scanf("%d",&work;)
   // scanf("%d %d %d %d",Ax,&Ay,&Bx,&By);
    result1=Ax*Ax+Ay*Ay;
      result2=Bx*Bx+By*By;
      if(result1>result2)
      {
        printf("%d",result2);
      }
      else
      printf("%d",result1);
return 0;
}